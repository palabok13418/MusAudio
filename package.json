{
  "name": "musaudio-website",
  "private": true,
  "version": "0.0.0",
  "description": "MusAudio — Website Version (static site + serverless proxy endpoints)",
  "license": "UNLICENSED",
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "vendor:fetch": "node scripts/vendor-fetch.mjs",
    "cloud:gen": "node -e \"console.log('cloud:gen disabled until further notice')\"",
    "prebuild": "node scripts/vendor-fetch.mjs",
    "build": "vite build && node scripts/ensure-dist-audio.mjs",
    "deploy:pages": "npx wrangler pages deploy dist",
    "prevercel-build": "node scripts/vendor-fetch.mjs",
    "vercel-build": "vite build && node scripts/ensure-dist-audio.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "predev": "node -e \"console.log('predev: cloud library disabled')\"",
    "dev": "http-server -c-1 -p 5173 .",
    "predev:vite": "node -e \"console.log('predev:vite: cloud library disabled')\"",
    "dev:vite": "vite --port 5174 --strictPort",
    "build:vite": "vite build",
    "preview:vite": "vite preview --port 5174 --strictPort"
  },
  "dependencies": {
    "@netlify/neon": "^0.1.0",
    "@vercel/analytics": "^1.6.1",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.3",
    "music-metadata": "^11.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^15.12.0",
    "http-server": "^14.1.1",
    "prettier": "^3.7.4",
    "vite": "5.4.19"
  }
}
